This Online Bin Packing scoring function raised a runtime error or division-by-zero!

Function:
{code}

Crash Report:
{crash_report}

TASK:
Fix the zero-division, log of negative number, or domain error in the mathematical expression. Ensure it safely returns a valid float score or -float('inf') when bin_remaining < item.

STRICT RULES:
1. NO IMPORTS: Do NOT use any 'import' statements.
2. SIGNATURE: Maintain exact signature: score_bin(bin_remaining, item, capacity, avg_bins_rem, n_open_bins).
3. SAFETY: Prevent zero-division by adding epsilon (e.g. + 1e-6) to denominators.

FIRST, write 1 sentence explaining the mathematical error and your fix.
THEN, return ONLY the patched code inside a single python code block.